# name: Release

# on:
#   push:
#     tags:
#       - 'v*'

# env:
#   CARGO_TERM_COLOR: always

# jobs:
#   build:
#     name: Build ${{ matrix.target }}
#     runs-on: ${{ matrix.os }}
#     strategy:
#       fail-fast: false
#       matrix:
#         include:
#           - target: x86_64-unknown-linux-gnu
#             os: ubuntu-latest
#             binary: iii-mcp
#             archive: iii-mcp-linux-x64.tar.gz
#           - target: aarch64-unknown-linux-gnu
#             os: ubuntu-latest
#             binary: iii-mcp
#             archive: iii-mcp-linux-arm64.tar.gz
#           - target: x86_64-apple-darwin
#             os: macos-latest
#             binary: iii-mcp
#             archive: iii-mcp-macos-x64.tar.gz
#           - target: aarch64-apple-darwin
#             os: macos-latest
#             binary: iii-mcp
#             archive: iii-mcp-macos-arm64.tar.gz
#           - target: x86_64-pc-windows-msvc
#             os: windows-latest
#             binary: iii-mcp.exe
#             archive: iii-mcp-windows-x64.zip

#     steps:
#       - uses: actions/checkout@v4

#       - name: Install Rust toolchain
#         uses: dtolnay/rust-action@stable
#         with:
#           targets: ${{ matrix.target }}

#       - name: Install cross-compilation tools (Linux ARM64)
#         if: matrix.target == 'aarch64-unknown-linux-gnu'
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y gcc-aarch64-linux-gnu

#       - name: Build
#         run: cargo build --release --target ${{ matrix.target }}

#       - name: Package (Unix)
#         if: runner.os != 'Windows'
#         run: |
#           cd target/${{ matrix.target }}/release
#           tar -czvf ../../../${{ matrix.archive }} ${{ matrix.binary }}
#           cd ../../..

#       - name: Package (Windows)
#         if: runner.os == 'Windows'
#         run: |
#           cd target/${{ matrix.target }}/release
#           7z a ../../../${{ matrix.archive }} ${{ matrix.binary }}
#           cd ../../..

#       - name: Upload artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: ${{ matrix.archive }}
#           path: ${{ matrix.archive }}

#   release:
#     name: Create Release
#     needs: build
#     runs-on: ubuntu-latest
#     permissions:
#       contents: write
#     steps:
#       - uses: actions/checkout@v4

#       - name: Download all artifacts
#         uses: actions/download-artifact@v4
#         with:
#           path: artifacts

#       - name: Create Release
#         uses: softprops/action-gh-release@v1
#         with:
#           files: artifacts/**/*
#           generate_release_notes: true
#           draft: false
#           prerelease: ${{ contains(github.ref, 'alpha') || contains(github.ref, 'beta') || contains(github.ref, 'rc') }}
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

#   publish-crates:
#     name: Publish to crates.io
#     needs: release
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - name: Install Rust toolchain
#         uses: dtolnay/rust-action@stable

#       - name: Publish to crates.io
#         run: cargo publish --token ${{ secrets.CARGO_REGISTRY_TOKEN }}
#         continue-on-error: true
